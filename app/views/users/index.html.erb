<% provide(:title, 'All users') %>

<div class="row">
  <h3 class="text-center">All users</h3>
    <%= will_paginate @users, :style => 'color:blue' %>
        <% @users.each do |user| %>
            <%= render user %>
    <% end %>
</div>